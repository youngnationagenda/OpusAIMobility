package com.google.maps.model;

/**
 * Stub — replaces the Google Maps Java Client server-side SDK.
 * The Android app uses com.google.android.gms:play-services-maps for
 * directions; this stub satisfies compile-time imports only.
 */
public class DirectionsResult {
    public DirectionsRoute[] routes = new DirectionsRoute[0];
}
